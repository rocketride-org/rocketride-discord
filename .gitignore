# dependencies
node_modules/

# secrets & local env
.env
.env.local

# runtime state & logs (pids, bot logs, paused-threads.json)
logs/

# RocketRide SDK docs fetched locally (see refresh-docs.sh)
.rocketride/

# private Slack routing directory (real member IDs) — commit slack-experts.example.json instead
data/slack-experts.json

# eval: local SQLite is under logs/ (already ignored); Phase 5 regenerated isolated pipe copies
pipelines/.eval/

# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db
